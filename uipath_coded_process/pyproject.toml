[project]
name = "launchkit-analyze"
version = "0.1.0"
description = "LaunchKit UiPath coded agents — coding CLI analyze + API proxies"
readme = "README.md"
authors = [{ name = "Sagar Sahu", email = "sahu.sagar0314@gmail.com" }]
requires-python = ">=3.11"
dependencies = [
    "uipath>=2.11.12",
    "httpx>=0.28.0",
]

[tool.pytest.ini_options]
pythonpath = ["."]
