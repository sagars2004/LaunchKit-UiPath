[project]
name = "launchkit-analyze"
version = "0.1.0"
description = "LaunchKit code analysis coded function — calls POST /analyze on the Render API"
readme = "README.md"
authors = [{ name = "Sagar Sahu", email = "sahu.sagar0314@gmail.com" }]
requires-python = ">=3.11"
dependencies = [
    "uipath>=2.11.12",
    "httpx>=0.28.0",
]
